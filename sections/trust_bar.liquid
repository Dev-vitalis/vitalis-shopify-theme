{% style %}
  .trust-bar { background: var(--color-bg-white); padding: 1.25rem 1rem; box-shadow: var(--shadow-sm); }
  .trust-bar .container { display: flex; flex-wrap: wrap; justify-content: center; gap: 2rem; align-items: center; }
  .trust-bar__item { display: flex; align-items: center; gap: 0.6rem; font-weight: 500; color: var(--color-text); }
  .trust-bar__icon { width: 40px; height: 40px; border-radius: 50%; background: var(--color-bg); color: var(--color-primary-light); display: flex; align-items: center; justify-content: center; font-size: 1.1rem; }
  @media (max-width: 600px) { .trust-bar .container { flex-direction: column; gap: 1rem; } }
{% endstyle %}

<section class="trust-bar">
  <div class="container">
    <div class="trust-bar__item">
      <span class="trust-bar__icon" aria-hidden="true">🚚</span>
      <span>{{ section.settings.text_shipping | default: 'Envío gratis en pedidos seleccionados' }}</span>
    </div>
    <div class="trust-bar__item">
      <span class="trust-bar__icon" aria-hidden="true">💳</span>
      <span>{{ section.settings.text_payment | default: 'Pago contra entrega en toda Colombia' }}</span>
    </div>
    <div class="trust-bar__item">
      <span class="trust-bar__icon" aria-hidden="true">✓</span>
      <span>{{ section.settings.text_trust | default: 'Productos de calidad para tu bienestar' }}</span>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Barra de confianza",
  "settings": [
    { "type": "text", "id": "text_shipping", "label": "Texto envío", "default": "Envío gratis en pedidos seleccionados" },
    { "type": "text", "id": "text_payment", "label": "Texto pago", "default": "Pago contra entrega en toda Colombia" },
    { "type": "text", "id": "text_trust", "label": "Texto confianza", "default": "Productos de calidad para tu bienestar" }
  ]
}
{% endschema %}
